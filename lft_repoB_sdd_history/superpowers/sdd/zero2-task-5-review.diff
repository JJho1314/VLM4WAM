--- a/scripts/qwen3_vl_semantic_planner/README.md
+++ b/scripts/qwen3_vl_semantic_planner/README.md
@@ -24,15 +24,26 @@
 
 Kept flat (not in a subdir) because it reuses the sibling shared infra (`qwen3vl_wrapper`, SigLIP builder).
 
-### 3. lingbot-DINO · 4B  (new)
+### 3. lingbot-DINO · 4B  (current FastWAM configuration)
 Full lingbot-vla-v2 recipe: base = Qwen3-VL-4B **extracted from the open `robbyant/lingbot-vla-v2-6b`**;
 target = **DINO-video** (1024-d, 256 tokens/keyframe), not SigLIP; head = faithful `TaskTokenResampler`
-warm-started from lingbot's `future_video_align_head`; loss = plain MSE. Plan = 5 keyframes × 256 =
-[B, 1280, 1024]. Trainer `train_qwen3vl4b_lingbot_dino_planner.py` (flat, reuses the shared data infra);
-4B-specific modules + launcher live in `lingbot_dino_4b/`.
+warm-started from lingbot's alignment heads; loss = plain MSE. Trainer
+`train_qwen3vl4b_lingbot_dino_planner.py` is flat because it reuses the shared data infrastructure;
+4B-specific modules and launchers live in `lingbot_dino_4b/`.
+
+Qwen3-VL 4B LingBot FastWAM current configuration:
+- frames: current 0 and future 8 from a 9-frame sample
+- VLM queries: 4 independent groups × 64 = 256 tokens
+- outputs: current/future DINO and current/future depth, each 256 × 1024
+- distributed runtime: Accelerate + DeepSpeed ZeRO-2
+- preferred batch: 8 GPUs × 8/GPU × accumulation 2 = global 128
+- generic launcher: `lingbot_dino_4b/train_lingbot_dino_4b.sh`
+- POD profile: `lingbot_dino_4b/train_lingbot_fastwam_pod.sh`
+- HPC3 profile: `lingbot_dino_4b/train_lingbot_fastwam_hpc3.sbatch`
+
 ```bash
-DATASET_ROOT=<droid dataset> bash lingbot_dino_4b/train_lingbot_dino_4b.sh          # full (2 GPU)
-NUM_GPUS=1 BATCH_SIZE=1 MAX_STEPS=2 FULL_FINETUNE=0 DATASET_ROOT=<...> \
+DATASET_ROOT=<droid dataset> bash lingbot_dino_4b/train_lingbot_dino_4b.sh          # generic
+USE_DEEPSPEED=0 NUM_GPUS=1 BATCH_SIZE=1 GRAD_ACCUM=1 EXPECTED_GLOBAL_BATCH=1 MAX_STEPS=2 SAVE_STEPS=2 FULL_FINETUNE=0 DATASET_ROOT=<...> \
   bash lingbot_dino_4b/train_lingbot_dino_4b.sh                                     # smoke
 ```
 Validated on this box: teacher import/build (parent-stub bypass for transformers 5.x), VLM extraction
@@ -59,9 +70,11 @@
 ├── sbatch_train_qwen3vl2b_*.sh               # launchers (lines 1-2)
 └── lingbot_dino_4b/                          # line 3 modules (lingbot-DINO 4B)
     ├── lingbot_resampler.py                  # verbatim TaskTokenResampler port (warm-start key-compat)
-    ├── lingbot_dino_head.py                  # 5-keyframe head [B,1280,1024]
+    ├── lingbot_dino_head.py                  # current/future DINO + depth heads
     ├── dino_video_target.py                  # DINO-video teacher target encoder (parent-stub import bypass)
     ├── extract_qwenvl_from_lingbot.py        # extract stock Qwen3-VL-4B from the 6b ckpt
-    ├── train_lingbot_dino_4b.sh              # self-contained torchrun launcher
+    ├── train_lingbot_dino_4b.sh              # generic Accelerate + ZeRO-2 launcher
+    ├── train_lingbot_fastwam_pod.sh           # POD profile
+    ├── train_lingbot_fastwam_hpc3.sbatch      # HPC3 profile
     └── LINGBOT_DINO_SPEC.md                   # spec + swap plan
 ```
--- a/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_current_future_fastwam_k1.sh
+++ b/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_current_future_fastwam_k1.sh
@@ -1,21 +0,0 @@
-#!/usr/bin/env bash
-set -euo pipefail
-
-SCRIPT_DIR=$(cd -- "$(dirname -- "$0")" && pwd)
-
-export USE_DEPTH=1
-export USE_CURRENT_ALIGNMENT=1
-export NUM_TASK_TOKENS=${NUM_TASK_TOKENS:-8}
-export SEQUENCE_LENGTH=9
-export NUM_KEYFRAMES=1
-export GRID_SIZE=16
-export SEMANTIC_DIM=1024
-export KEYFRAME_SCHEME=even_future
-export CURRENT_DINO_LOSS_WEIGHT=${CURRENT_DINO_LOSS_WEIGHT:-0.004}
-export FUTURE_DINO_LOSS_WEIGHT=${FUTURE_DINO_LOSS_WEIGHT:-0.004}
-export CURRENT_DEPTH_LOSS_WEIGHT=${CURRENT_DEPTH_LOSS_WEIGHT:-0.004}
-export FUTURE_DEPTH_LOSS_WEIGHT=${FUTURE_DEPTH_LOSS_WEIGHT:-0.004}
-export FASTWAM_DATA_CONFIG=${FASTWAM_DATA_CONFIG:-third_party/FastWAM/configs/data/libero_2cam_cosmos.yaml}
-export OUTPUT_DIR=${OUTPUT_DIR:-outputs/qwen3vl4b_lingbot_current_future_fastwam_k1}
-
-exec "$SCRIPT_DIR/train_lingbot_dino_4b.sh" "$@"
--- a/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_current_future_fastwam_k1_pod30274.sh
+++ b/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_current_future_fastwam_k1_pod30274.sh
@@ -1,88 +0,0 @@
-#!/usr/bin/env bash
-set -euo pipefail
-
-ROOT=/root/nas/junjie
-REPO_ROOT=$ROOT/code/VLM4WAM_k1_fastwam_20260712
-DATA_ROOT=$ROOT/data/LIBERO-fastwam
-WEIGHTS=$ROOT/weights
-PY=$ROOT/conda_envs/vlm4wam/bin/python
-
-RUN_KIND=${RUN_KIND:-formal}
-NUM_GPUS=8
-BATCH_SIZE=${BATCH_SIZE:-16}
-GRAD_ACCUM=${GRAD_ACCUM:-1}
-MAX_STEPS=${MAX_STEPS:-12000}
-SAVE_STEPS=${SAVE_STEPS:-1000}
-GLOBAL_BATCH_SIZE=$((NUM_GPUS * BATCH_SIZE * GRAD_ACCUM))
-
-if [[ "$GLOBAL_BATCH_SIZE" -ne 128 ]]; then
-  echo "ERROR: expected global batch 128, got $GLOBAL_BATCH_SIZE" >&2
-  exit 2
-fi
-
-for path in \
-  "$PY" \
-  "$WEIGHTS/Qwen3-VL-4B-lingbot-vlm" \
-  "$WEIGHTS/lingbot_align_heads_warmstart/model.safetensors.index.json" \
-  "$WEIGHTS/lingbot-vla-v2-6b/dino_video/teacher_step_10000.pth" \
-  "$WEIGHTS/lingbot-vla-v2-6b/depth/model.pt" \
-  "$WEIGHTS/moge-2-vitb-normal/model.pt" \
-  "$ROOT/data/LIBERO-fastwam_meta/dataset_stats.json" \
-  "$ROOT/data/libero_qwen" \
-  "$DATA_ROOT/libero_spatial_no_noops_lerobot" \
-  "$DATA_ROOT/libero_object_no_noops_lerobot" \
-  "$DATA_ROOT/libero_goal_no_noops_lerobot" \
-  "$DATA_ROOT/libero_10_no_noops_lerobot"; do
-  if [[ ! -e "$path" ]]; then
-    echo "ERROR: missing required path: $path" >&2
-    exit 2
-  fi
-done
-
-if [[ "$RUN_KIND" == "smoke" ]]; then
-  OUTPUT_DIR=$REPO_ROOT/outputs/smoke_k1_current_future_8gpu_b4a4
-else
-  OUTPUT_DIR=$REPO_ROOT/outputs/qwen3vl4b_lingbot_current_future_fastwam_k1_8gpu_gbs128_12k
-fi
-LOG_DIR=$REPO_ROOT/logs
-mkdir -p "$OUTPUT_DIR" "$LOG_DIR" "$ROOT/cache/triton" "$ROOT/cache/inductor"
-
-export CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6,7
-export HF_HUB_OFFLINE=1
-export TRANSFORMERS_OFFLINE=1
-export TOKENIZERS_PARALLELISM=false
-export PLANNER_WANDB=0
-export TRITON_CACHE_DIR=$ROOT/cache/triton
-export TORCHINDUCTOR_CACHE_DIR=$ROOT/cache/inductor
-export OMP_NUM_THREADS=4
-
-cd "$REPO_ROOT"
-echo "[pod] run=$RUN_KIND gpu=8 batch=$BATCH_SIZE accum=$GRAD_ACCUM global=$GLOBAL_BATCH_SIZE"
-echo "[contract] frames=9 offset=8 current8+future8 output=256 four_losses=0.004"
-
-NUM_GPUS=$NUM_GPUS \
-BATCH_SIZE=$BATCH_SIZE \
-GRAD_ACCUM=$GRAD_ACCUM \
-MAX_STEPS=$MAX_STEPS \
-SAVE_STEPS=$SAVE_STEPS \
-LOG_STEPS=10 \
-FULL_FINETUNE=1 \
-NUM_WORKERS=4 \
-LR=3e-5 \
-HEAD_LR=3e-4 \
-WARMUP_STEPS=1000 \
-PY=$PY \
-WEIGHTS=$WEIGHTS \
-MODEL_PATH=$WEIGHTS/Qwen3-VL-4B-lingbot-vlm \
-LINGBOT_6B=$WEIGHTS/lingbot-vla-v2-6b \
-HEAD_WARMSTART_CKPT=$WEIGHTS/lingbot_align_heads_warmstart \
-DEPTH_MOGE_PATH=$WEIGHTS/moge-2-vitb-normal/model.pt \
-DEPTH_MORGBD_PATH=$WEIGHTS/lingbot-vla-v2-6b/depth/model.pt \
-LINGBOT_SRC_ROOT=$ROOT/code/lingbot-vla-v2 \
-UTILS3D_MOGE_PATH=$ROOT/py_deps/utils3d_moge \
-FASTWAM_DATA_CONFIG=third_party/FastWAM/configs/data/libero_2cam_cosmos.yaml \
-FASTWAM_DATASET_DIRS=$DATA_ROOT/libero_spatial_no_noops_lerobot:$DATA_ROOT/libero_object_no_noops_lerobot:$DATA_ROOT/libero_goal_no_noops_lerobot:$DATA_ROOT/libero_10_no_noops_lerobot \
-FASTWAM_TEXT_EMBEDDING_CACHE_DIR=$ROOT/data/libero_qwen \
-FASTWAM_PRETRAINED_NORM_STATS=$ROOT/data/LIBERO-fastwam_meta/dataset_stats.json \
-OUTPUT_DIR=$OUTPUT_DIR \
-bash scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_current_future_fastwam_k1.sh
--- a/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_current_future_fastwam_k1_hpc3.sbatch
+++ b/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_current_future_fastwam_k1_hpc3.sbatch
@@ -1,107 +0,0 @@
-#!/usr/bin/env bash
-# LingBot-native current + one-future planner training on HPC3 (jhe724).
-# Global batch = 8 GPUs * 4 samples/GPU * 4 accumulation = 128.
-# If batch 4 OOMs, submit with BATCH_SIZE=2,GRAD_ACCUM=8.
-#SBATCH -J vlmp_k1_cf_g128
-#SBATCH -p acd_u
-#SBATCH --gres=gpu:8
-#SBATCH -N 1
-#SBATCH --ntasks=1
-#SBATCH --cpus-per-task=64
-#SBATCH -t 2-00:00:00
-#SBATCH -o /data/user/jhe724/workspace/VLM4WAM_lingbot_k1_current_20260712/logs/vlm_planner_k1_cf_%j.log
-
-set -euo pipefail
-
-WORKSPACE=/data/user/jhe724/workspace
-REPO_ROOT=$WORKSPACE/VLM4WAM_lingbot_k1_current_20260712
-DATA_ROOT=$WORKSPACE/datasets/LIBERO-fastwam
-TEXT_CACHE=$WORKSPACE/datasets/libero_qwen
-NORM_STATS=$WORKSPACE/datasets/LIBERO-fastwam_meta/dataset_stats.json
-WEIGHTS=$WORKSPACE/weights
-
-RUN_KIND=${RUN_KIND:-formal}
-NUM_GPUS=8
-BATCH_SIZE=${BATCH_SIZE:-4}
-GRAD_ACCUM=${GRAD_ACCUM:-4}
-MAX_STEPS=${MAX_STEPS:-12000}
-SAVE_STEPS=${SAVE_STEPS:-1000}
-LOG_STEPS=${LOG_STEPS:-10}
-GLOBAL_BATCH_SIZE=$((NUM_GPUS * BATCH_SIZE * GRAD_ACCUM))
-
-if [[ "$GLOBAL_BATCH_SIZE" -ne 128 ]]; then
-  echo "ERROR: expected global batch 128, got $GLOBAL_BATCH_SIZE" >&2
-  exit 2
-fi
-
-for required_path in \
-  "$REPO_ROOT/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_current_future_fastwam_k1.sh" \
-  "$REPO_ROOT/third_party/FastWAM/configs/data/libero_2cam_cosmos.yaml" \
-  "$DATA_ROOT/libero_spatial_no_noops_lerobot" \
-  "$DATA_ROOT/libero_object_no_noops_lerobot" \
-  "$DATA_ROOT/libero_goal_no_noops_lerobot" \
-  "$DATA_ROOT/libero_10_no_noops_lerobot" \
-  "$TEXT_CACHE" \
-  "$NORM_STATS" \
-  "$WEIGHTS/Qwen3-VL-4B-lingbot-vlm" \
-  "$WEIGHTS/lingbot_align_heads_warmstart/model.safetensors.index.json" \
-  "$WEIGHTS/lingbot-vla-v2-6b/dino_video/teacher_step_10000.pth" \
-  "$WEIGHTS/lingbot-vla-v2-6b/depth/model.pt" \
-  "$WEIGHTS/moge-2-vitb-normal/model.pt"; do
-  if [[ ! -e "$required_path" ]]; then
-    echo "ERROR: missing required path: $required_path" >&2
-    exit 2
-  fi
-done
-
-if [[ "$RUN_KIND" == "smoke" ]]; then
-  OUTPUT_DIR=$REPO_ROOT/outputs/smoke_k1_cf_b${BATCH_SIZE}_a${GRAD_ACCUM}_job${SLURM_JOB_ID}
-else
-  OUTPUT_DIR=$REPO_ROOT/outputs/qwen3vl4b_lingbot_current_future_k1_fullft_b${BATCH_SIZE}_a${GRAD_ACCUM}_gbs128_${MAX_STEPS}step
-fi
-
-export HF_HUB_OFFLINE=1
-export TRANSFORMERS_OFFLINE=1
-export TOKENIZERS_PARALLELISM=false
-export CC=/usr/bin/gcc
-export CXX=/usr/bin/g++
-export TRITON_CACHE_DIR=$WORKSPACE/.cache/triton
-export TORCHINDUCTOR_CACHE_DIR=$WORKSPACE/.cache/inductor
-export OMP_NUM_THREADS=4
-mkdir -p "$REPO_ROOT/logs" "$OUTPUT_DIR" "$TRITON_CACHE_DIR" "$TORCHINDUCTOR_CACHE_DIR"
-
-cd "$REPO_ROOT"
-echo "[slurm] job_id=$SLURM_JOB_ID host=$(hostname) run_kind=$RUN_KIND"
-nvidia-smi --query-gpu=index,name,memory.total --format=csv,noheader
-echo "[batch] gpu=8 per_gpu=$BATCH_SIZE accum=$GRAD_ACCUM global=128"
-echo "[train] steps=$MAX_STEPS lr=3e-5 head_lr=3e-4 warmup=1000 cosine bf16 full_ft"
-echo "[contract] video_frames=9 future_offset=8 future_time=1.0 task_queries=current8+future8 output_tokens=256"
-echo "[loss] current_dino=0.004 future_dino=0.004 current_depth=0.004 future_depth=0.004"
-echo "[output] $OUTPUT_DIR"
-
-NUM_GPUS=$NUM_GPUS \
-BATCH_SIZE=$BATCH_SIZE \
-GRAD_ACCUM=$GRAD_ACCUM \
-MAX_STEPS=$MAX_STEPS \
-SAVE_STEPS=$SAVE_STEPS \
-LOG_STEPS=$LOG_STEPS \
-FULL_FINETUNE=1 \
-NUM_WORKERS=4 \
-LR=3e-5 \
-HEAD_LR=3e-4 \
-WARMUP_STEPS=1000 \
-PY=$HOME/.conda/envs/starVLA/bin/python \
-WEIGHTS=$WEIGHTS \
-MODEL_PATH=$WEIGHTS/Qwen3-VL-4B-lingbot-vlm \
-LINGBOT_6B=$WEIGHTS/lingbot-vla-v2-6b \
-HEAD_WARMSTART_CKPT=$WEIGHTS/lingbot_align_heads_warmstart \
-DEPTH_MOGE_PATH=$WEIGHTS/moge-2-vitb-normal/model.pt \
-DEPTH_MORGBD_PATH=$WEIGHTS/lingbot-vla-v2-6b/depth/model.pt \
-LINGBOT_SRC_ROOT=$WORKSPACE/lingbot-vla-v2 \
-UTILS3D_MOGE_PATH=$WORKSPACE/py_deps/utils3d_moge \
-FASTWAM_DATA_CONFIG=third_party/FastWAM/configs/data/libero_2cam_cosmos.yaml \
-FASTWAM_DATASET_DIRS=$DATA_ROOT/libero_spatial_no_noops_lerobot:$DATA_ROOT/libero_object_no_noops_lerobot:$DATA_ROOT/libero_goal_no_noops_lerobot:$DATA_ROOT/libero_10_no_noops_lerobot \
-FASTWAM_TEXT_EMBEDDING_CACHE_DIR=$TEXT_CACHE \
-FASTWAM_PRETRAINED_NORM_STATS=$NORM_STATS \
-OUTPUT_DIR=$OUTPUT_DIR \
-bash scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_current_future_fastwam_k1.sh
--- a/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_dino_depth_fastwam_k4.sh
+++ b/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_dino_depth_fastwam_k4.sh
@@ -1,17 +0,0 @@
-#!/usr/bin/env bash
-set -euo pipefail
-
-SCRIPT_DIR=$(cd -- "$(dirname -- "$0")" && pwd)
-
-export USE_DEPTH=1
-export SEQUENCE_LENGTH=9
-export NUM_KEYFRAMES=4
-export GRID_SIZE=16
-export SEMANTIC_DIM=1024
-export KEYFRAME_SCHEME=even_future
-export SHARED_LATENT_PER_KEYFRAME=32
-export PRIVATE_LATENT_PER_KEYFRAME=32
-export FASTWAM_DATA_CONFIG=${FASTWAM_DATA_CONFIG:-third_party/FastWAM/configs/data/libero_2cam_cosmos.yaml}
-export OUTPUT_DIR=${OUTPUT_DIR:-outputs/qwen3vl4b_lingbot_dino_depth_fastwam_k4}
-
-exec "$SCRIPT_DIR/train_lingbot_dino_4b.sh" "$@"
--- a/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_dino_depth_fastwam_k4_hpc3.sbatch
+++ b/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_dino_depth_fastwam_k4_hpc3.sbatch
@@ -1,118 +0,0 @@
-#!/usr/bin/env bash
-# FastWAM-aligned 4B VLM planner training on one HPC3 8-GPU node.
-#
-# The batch decomposition is supplied at submission time, but this launcher
-# refuses to run unless the effective global batch is exactly 128.
-#
-# Smoke (preferred throughput setting):
-#   sbatch --export=ALL,RUN_KIND=smoke,BATCH_SIZE=4,GRAD_ACCUM=4,MAX_STEPS=2,SAVE_STEPS=2 \
-#     scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_dino_depth_fastwam_k4_hpc3.sbatch
-# Formal:
-#   sbatch --export=ALL,RUN_KIND=formal,BATCH_SIZE=4,GRAD_ACCUM=4,MAX_STEPS=12000 \
-#     scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_dino_depth_fastwam_k4_hpc3.sbatch
-#SBATCH -J vlmp_k4_g128
-#SBATCH -p acd_u
-#SBATCH --gres=gpu:8
-#SBATCH -N 1
-#SBATCH --ntasks=1
-#SBATCH --cpus-per-task=64
-#SBATCH -t 2-00:00:00
-#SBATCH -o /data/user/jhe724/workspace/VLM4WAM_k4_gbs128_20260711/logs/vlm_planner_k4_gbs128_%j.log
-
-set -euo pipefail
-
-WORKSPACE=/data/user/jhe724/workspace
-REPO_ROOT=$WORKSPACE/VLM4WAM_k4_gbs128_20260711
-DATA_ROOT=$WORKSPACE/datasets/LIBERO-fastwam
-TEXT_CACHE=$WORKSPACE/datasets/libero_qwen
-NORM_STATS=$WORKSPACE/datasets/LIBERO-fastwam_meta/dataset_stats.json
-WEIGHTS=$WORKSPACE/weights
-
-RUN_KIND=${RUN_KIND:-formal}
-NUM_GPUS=${NUM_GPUS:-8}
-BATCH_SIZE=${BATCH_SIZE:-4}
-GRAD_ACCUM=${GRAD_ACCUM:-4}
-MAX_STEPS=${MAX_STEPS:-12000}
-SAVE_STEPS=${SAVE_STEPS:-1000}
-LOG_STEPS=${LOG_STEPS:-10}
-GLOBAL_BATCH_SIZE=$((NUM_GPUS * BATCH_SIZE * GRAD_ACCUM))
-
-if [[ "$NUM_GPUS" -ne 8 ]]; then
-  echo "ERROR: this launcher requires exactly 8 GPUs, got $NUM_GPUS" >&2
-  exit 2
-fi
-if [[ "$GLOBAL_BATCH_SIZE" -ne 128 ]]; then
-  echo "ERROR: expected global batch 128, got $GLOBAL_BATCH_SIZE" >&2
-  exit 2
-fi
-
-for required_path in \
-  "$REPO_ROOT/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_dino_depth_fastwam_k4.sh" \
-  "$REPO_ROOT/third_party/FastWAM/configs/data/libero_2cam_cosmos.yaml" \
-  "$DATA_ROOT/libero_spatial_no_noops_lerobot" \
-  "$DATA_ROOT/libero_object_no_noops_lerobot" \
-  "$DATA_ROOT/libero_goal_no_noops_lerobot" \
-  "$DATA_ROOT/libero_10_no_noops_lerobot" \
-  "$TEXT_CACHE" \
-  "$NORM_STATS" \
-  "$WEIGHTS/Qwen3-VL-4B-lingbot-vlm" \
-  "$WEIGHTS/lingbot-vla-v2-6b/dino_video/teacher_step_10000.pth" \
-  "$WEIGHTS/lingbot-vla-v2-6b/depth/model.pt" \
-  "$WEIGHTS/moge-2-vitb-normal/model.pt"; do
-  if [[ ! -e "$required_path" ]]; then
-    echo "ERROR: missing required path: $required_path" >&2
-    exit 2
-  fi
-done
-
-if [[ "$RUN_KIND" == "smoke" ]]; then
-  OUTPUT_DIR=$REPO_ROOT/outputs/smoke_k4_b${BATCH_SIZE}_a${GRAD_ACCUM}_gbs128_job${SLURM_JOB_ID}
-else
-  OUTPUT_DIR=$REPO_ROOT/outputs/qwen3vl4b_lingbot_dino_depth_fastwam_k4_fullft_b${BATCH_SIZE}_a${GRAD_ACCUM}_gbs128_${MAX_STEPS}step
-fi
-
-export HF_HUB_OFFLINE=1
-export TRANSFORMERS_OFFLINE=1
-export TOKENIZERS_PARALLELISM=false
-export CC=/usr/bin/gcc
-export CXX=/usr/bin/g++
-export TRITON_CACHE_DIR=$WORKSPACE/.cache/triton
-export TORCHINDUCTOR_CACHE_DIR=$WORKSPACE/.cache/inductor
-export OMP_NUM_THREADS=4
-mkdir -p "$REPO_ROOT/logs" "$OUTPUT_DIR" "$TRITON_CACHE_DIR" "$TORCHINDUCTOR_CACHE_DIR"
-
-cd "$REPO_ROOT"
-echo "[slurm] job_id=$SLURM_JOB_ID host=$(hostname) run_kind=$RUN_KIND"
-nvidia-smi --query-gpu=index,name,memory.total --format=csv,noheader
-echo "[batch] num_gpus=$NUM_GPUS batch_per_gpu=$BATCH_SIZE grad_accum=$GRAD_ACCUM global_batch=$GLOBAL_BATCH_SIZE"
-echo "[train] max_steps=$MAX_STEPS lr=3e-5 head_lr=3e-4 warmup_steps=1000 full_finetune=1 dtype=bf16"
-echo "[contract] sequence_length=9 keyframes=4 offsets=2,4,6,8 queries=32_shared+32_private_per_branch total_unique_queries=384"
-echo "[output] $OUTPUT_DIR"
-
-NUM_GPUS=$NUM_GPUS \
-BATCH_SIZE=$BATCH_SIZE \
-GRAD_ACCUM=$GRAD_ACCUM \
-MAX_STEPS=$MAX_STEPS \
-SAVE_STEPS=$SAVE_STEPS \
-LOG_STEPS=$LOG_STEPS \
-FULL_FINETUNE=1 \
-USE_DEPTH=1 \
-NUM_WORKERS=4 \
-LR=3e-5 \
-HEAD_LR=3e-4 \
-WARMUP_STEPS=1000 \
-PY=$HOME/.conda/envs/starVLA/bin/python \
-WEIGHTS=$WEIGHTS \
-MODEL_PATH=$WEIGHTS/Qwen3-VL-4B-lingbot-vlm \
-LINGBOT_6B=$WEIGHTS/lingbot-vla-v2-6b \
-HEAD_WARMSTART_CKPT=$WEIGHTS/lingbot_align_heads_warmstart \
-DEPTH_MOGE_PATH=$WEIGHTS/moge-2-vitb-normal/model.pt \
-DEPTH_MORGBD_PATH=$WEIGHTS/lingbot-vla-v2-6b/depth/model.pt \
-LINGBOT_SRC_ROOT=$WORKSPACE/lingbot-vla-v2 \
-UTILS3D_MOGE_PATH=$WORKSPACE/py_deps/utils3d_moge \
-FASTWAM_DATA_CONFIG=third_party/FastWAM/configs/data/libero_2cam_cosmos.yaml \
-FASTWAM_DATASET_DIRS=$DATA_ROOT/libero_spatial_no_noops_lerobot:$DATA_ROOT/libero_object_no_noops_lerobot:$DATA_ROOT/libero_goal_no_noops_lerobot:$DATA_ROOT/libero_10_no_noops_lerobot \
-FASTWAM_TEXT_EMBEDDING_CACHE_DIR=$TEXT_CACHE \
-FASTWAM_PRETRAINED_NORM_STATS=$NORM_STATS \
-OUTPUT_DIR=$OUTPUT_DIR \
-bash scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_dino_depth_fastwam_k4.sh
--- a/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_independent_queries_fastwam_k1_pod30274.sh
+++ b/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_independent_queries_fastwam_k1_pod30274.sh
@@ -1,91 +0,0 @@
-#!/usr/bin/env bash
-set -euo pipefail
-
-ROOT=/root/nas/junjie
-REPO_ROOT=$ROOT/code/VLM4WAM_k1_fastwam_20260712
-DATA_ROOT=$ROOT/data/LIBERO-fastwam
-WEIGHTS=$ROOT/weights
-PY=${PY:-$ROOT/conda_envs/vlm4wam/bin/python}
-
-RUN_KIND=${RUN_KIND:-formal}
-NUM_GPUS=8
-NUM_TASK_TOKENS=64
-BATCH_SIZE=${BATCH_SIZE:-4}
-GRAD_ACCUM=${GRAD_ACCUM:-4}
-MAX_STEPS=${MAX_STEPS:-12000}
-SAVE_STEPS=${SAVE_STEPS:-1000}
-GLOBAL_BATCH_SIZE=$((NUM_GPUS * BATCH_SIZE * GRAD_ACCUM))
-
-if [[ "$GLOBAL_BATCH_SIZE" -ne 128 ]]; then
-  echo "ERROR: expected global batch 128, got $GLOBAL_BATCH_SIZE" >&2
-  exit 2
-fi
-
-for path in \
-  "$PY" \
-  "$WEIGHTS/Qwen3-VL-4B-lingbot-vlm" \
-  "$WEIGHTS/lingbot_align_heads_warmstart/model.safetensors.index.json" \
-  "$WEIGHTS/lingbot-vla-v2-6b/dino_video/teacher_step_10000.pth" \
-  "$WEIGHTS/lingbot-vla-v2-6b/depth/model.pt" \
-  "$WEIGHTS/moge-2-vitb-normal/model.pt" \
-  "$ROOT/data/LIBERO-fastwam_meta/dataset_stats.json" \
-  "$ROOT/data/libero_qwen" \
-  "$DATA_ROOT/libero_spatial_no_noops_lerobot" \
-  "$DATA_ROOT/libero_object_no_noops_lerobot" \
-  "$DATA_ROOT/libero_goal_no_noops_lerobot" \
-  "$DATA_ROOT/libero_10_no_noops_lerobot"; do
-  if [[ ! -e "$path" ]]; then
-    echo "ERROR: missing required path: $path" >&2
-    exit 2
-  fi
-done
-
-if [[ "$RUN_KIND" == "smoke" ]]; then
-  OUTPUT_DIR=$REPO_ROOT/outputs/smoke_k1_independent_queries_q64_8gpu_b${BATCH_SIZE}a${GRAD_ACCUM}
-else
-  OUTPUT_DIR=$REPO_ROOT/outputs/qwen3vl4b_lingbot_independent_queries_q64_fastwam_k1_8gpu_b4a4_gbs128_12k
-fi
-mkdir -p "$OUTPUT_DIR" "$REPO_ROOT/logs" "$ROOT/cache/triton" "$ROOT/cache/inductor"
-
-export CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6,7
-export HF_HUB_OFFLINE=1
-export TRANSFORMERS_OFFLINE=1
-export TOKENIZERS_PARALLELISM=false
-export PLANNER_WANDB=0
-export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
-export TRITON_CACHE_DIR=$ROOT/cache/triton
-export TORCHINDUCTOR_CACHE_DIR=$ROOT/cache/inductor
-export OMP_NUM_THREADS=4
-export INDEPENDENT_MODALITY_TASK_TOKENS=1
-
-cd "$REPO_ROOT"
-echo "[pod] run=$RUN_KIND gpu=8 batch=$BATCH_SIZE accum=$GRAD_ACCUM global=$GLOBAL_BATCH_SIZE"
-echo "[contract] frames=9 offset=8 four_independent_groups=4x64 latent_len=256 output_tokens_per_feature=256 four_losses=0.004"
-
-NUM_GPUS=$NUM_GPUS \
-NUM_TASK_TOKENS=$NUM_TASK_TOKENS \
-BATCH_SIZE=$BATCH_SIZE \
-GRAD_ACCUM=$GRAD_ACCUM \
-MAX_STEPS=$MAX_STEPS \
-SAVE_STEPS=$SAVE_STEPS \
-LOG_STEPS=10 \
-FULL_FINETUNE=1 \
-NUM_WORKERS=4 \
-LR=3e-5 \
-HEAD_LR=3e-4 \
-WARMUP_STEPS=1000 \
-PY=$PY \
-WEIGHTS=$WEIGHTS \
-MODEL_PATH=$WEIGHTS/Qwen3-VL-4B-lingbot-vlm \
-LINGBOT_6B=$WEIGHTS/lingbot-vla-v2-6b \
-HEAD_WARMSTART_CKPT=$WEIGHTS/lingbot_align_heads_warmstart \
-DEPTH_MOGE_PATH=$WEIGHTS/moge-2-vitb-normal/model.pt \
-DEPTH_MORGBD_PATH=$WEIGHTS/lingbot-vla-v2-6b/depth/model.pt \
-LINGBOT_SRC_ROOT=$ROOT/code/lingbot-vla-v2 \
-UTILS3D_MOGE_PATH=$ROOT/py_deps/utils3d_moge \
-FASTWAM_DATA_CONFIG=third_party/FastWAM/configs/data/libero_2cam_cosmos.yaml \
-FASTWAM_DATASET_DIRS=$DATA_ROOT/libero_spatial_no_noops_lerobot:$DATA_ROOT/libero_object_no_noops_lerobot:$DATA_ROOT/libero_goal_no_noops_lerobot:$DATA_ROOT/libero_10_no_noops_lerobot \
-FASTWAM_TEXT_EMBEDDING_CACHE_DIR=$ROOT/data/libero_qwen \
-FASTWAM_PRETRAINED_NORM_STATS=$ROOT/data/LIBERO-fastwam_meta/dataset_stats.json \
-OUTPUT_DIR=$OUTPUT_DIR \
-bash scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_current_future_fastwam_k1.sh
--- a/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_independent_queries_fastwam_k1_hpc3.sbatch
+++ b/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_independent_queries_fastwam_k1_hpc3.sbatch
@@ -1,118 +0,0 @@
-#!/usr/bin/env bash
-# LingBot-native current + one-future planner with four independent modality/time
-# task-token groups on HPC3 (jhe724).
-# Global batch = 8 GPUs * 16 samples/GPU * 1 accumulation = 128.
-#
-# Smoke:
-#   sbatch --export=ALL,RUN_KIND=smoke,MAX_STEPS=2,SAVE_STEPS=2 \
-#     scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_independent_queries_fastwam_k1_hpc3.sbatch
-# Formal:
-#   sbatch --export=ALL,RUN_KIND=formal,MAX_STEPS=12000,SAVE_STEPS=1000 \
-#     scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_independent_queries_fastwam_k1_hpc3.sbatch
-#SBATCH -J vlmp_k1_indq_g128
-#SBATCH -p acd_u
-#SBATCH --gres=gpu:8
-#SBATCH -N 1
-#SBATCH --ntasks=1
-#SBATCH --cpus-per-task=64
-#SBATCH -t 2-00:00:00
-#SBATCH -o /data/user/jhe724/workspace/VLM4WAM_lingbot_k1_current_20260712/logs/vlm_planner_k1_indq_%j.log
-
-set -euo pipefail
-
-WORKSPACE=/data/user/jhe724/workspace
-REPO_ROOT=$WORKSPACE/VLM4WAM_lingbot_k1_current_20260712
-DATA_ROOT=$WORKSPACE/datasets/LIBERO-fastwam
-TEXT_CACHE=$WORKSPACE/datasets/libero_qwen
-NORM_STATS=$WORKSPACE/datasets/LIBERO-fastwam_meta/dataset_stats.json
-WEIGHTS=$WORKSPACE/weights
-
-RUN_KIND=${RUN_KIND:-formal}
-NUM_GPUS=8
-NUM_TASK_TOKENS=64
-BATCH_SIZE=${BATCH_SIZE:-16}
-GRAD_ACCUM=${GRAD_ACCUM:-1}
-MAX_STEPS=${MAX_STEPS:-12000}
-SAVE_STEPS=${SAVE_STEPS:-1000}
-LOG_STEPS=${LOG_STEPS:-10}
-GLOBAL_BATCH_SIZE=$((NUM_GPUS * BATCH_SIZE * GRAD_ACCUM))
-
-if [[ "$GLOBAL_BATCH_SIZE" -ne 128 ]]; then
-  echo "ERROR: expected global batch 128, got $GLOBAL_BATCH_SIZE" >&2
-  exit 2
-fi
-
-for required_path in \
-  "$REPO_ROOT/scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_current_future_fastwam_k1.sh" \
-  "$REPO_ROOT/third_party/FastWAM/configs/data/libero_2cam_cosmos.yaml" \
-  "$DATA_ROOT/libero_spatial_no_noops_lerobot" \
-  "$DATA_ROOT/libero_object_no_noops_lerobot" \
-  "$DATA_ROOT/libero_goal_no_noops_lerobot" \
-  "$DATA_ROOT/libero_10_no_noops_lerobot" \
-  "$TEXT_CACHE" \
-  "$NORM_STATS" \
-  "$WEIGHTS/Qwen3-VL-4B-lingbot-vlm" \
-  "$WEIGHTS/lingbot_align_heads_warmstart/model.safetensors.index.json" \
-  "$WEIGHTS/lingbot-vla-v2-6b/dino_video/teacher_step_10000.pth" \
-  "$WEIGHTS/lingbot-vla-v2-6b/depth/model.pt" \
-  "$WEIGHTS/moge-2-vitb-normal/model.pt"; do
-  if [[ ! -e "$required_path" ]]; then
-    echo "ERROR: missing required path: $required_path" >&2
-    exit 2
-  fi
-done
-
-if [[ "$RUN_KIND" == "smoke" ]]; then
-  OUTPUT_DIR=$REPO_ROOT/outputs/smoke_k1_independent_queries_q64_b${BATCH_SIZE}_a${GRAD_ACCUM}_job${SLURM_JOB_ID}
-else
-  OUTPUT_DIR=$REPO_ROOT/outputs/qwen3vl4b_lingbot_independent_queries_q64_fastwam_k1_8gpu_b${BATCH_SIZE}_a${GRAD_ACCUM}_gbs128_${MAX_STEPS}step
-fi
-
-export HF_HUB_OFFLINE=1
-export TRANSFORMERS_OFFLINE=1
-export TOKENIZERS_PARALLELISM=false
-export PLANNER_WANDB=0
-export CC=/usr/bin/gcc
-export CXX=/usr/bin/g++
-export TRITON_CACHE_DIR=$WORKSPACE/.cache/triton
-export TORCHINDUCTOR_CACHE_DIR=$WORKSPACE/.cache/inductor
-export OMP_NUM_THREADS=4
-export INDEPENDENT_MODALITY_TASK_TOKENS=1
-mkdir -p "$REPO_ROOT/logs" "$OUTPUT_DIR" "$TRITON_CACHE_DIR" "$TORCHINDUCTOR_CACHE_DIR"
-
-cd "$REPO_ROOT"
-echo "[slurm] job_id=$SLURM_JOB_ID host=$(hostname) run_kind=$RUN_KIND"
-nvidia-smi --query-gpu=index,name,memory.total --format=csv,noheader
-echo "[batch] gpu=$NUM_GPUS per_gpu=$BATCH_SIZE accum=$GRAD_ACCUM global=$GLOBAL_BATCH_SIZE"
-echo "[train] steps=$MAX_STEPS lr=3e-5 head_lr=3e-4 warmup=1000 cosine bf16 full_ft gradient_checkpointing=off"
-echo "[contract] frames=9 offset=8 four_independent_groups=4x64 latent_len=256 output_tokens_per_feature=256"
-echo "[loss] current_dino=0.004 future_dino=0.004 current_depth=0.004 future_depth=0.004"
-echo "[output] $OUTPUT_DIR"
-
-NUM_GPUS=$NUM_GPUS \
-NUM_TASK_TOKENS=$NUM_TASK_TOKENS \
-BATCH_SIZE=$BATCH_SIZE \
-GRAD_ACCUM=$GRAD_ACCUM \
-MAX_STEPS=$MAX_STEPS \
-SAVE_STEPS=$SAVE_STEPS \
-LOG_STEPS=$LOG_STEPS \
-FULL_FINETUNE=1 \
-NUM_WORKERS=4 \
-LR=3e-5 \
-HEAD_LR=3e-4 \
-WARMUP_STEPS=1000 \
-PY=$HOME/.conda/envs/starVLA/bin/python \
-WEIGHTS=$WEIGHTS \
-MODEL_PATH=$WEIGHTS/Qwen3-VL-4B-lingbot-vlm \
-LINGBOT_6B=$WEIGHTS/lingbot-vla-v2-6b \
-HEAD_WARMSTART_CKPT=$WEIGHTS/lingbot_align_heads_warmstart \
-DEPTH_MOGE_PATH=$WEIGHTS/moge-2-vitb-normal/model.pt \
-DEPTH_MORGBD_PATH=$WEIGHTS/lingbot-vla-v2-6b/depth/model.pt \
-LINGBOT_SRC_ROOT=$WORKSPACE/lingbot-vla-v2 \
-UTILS3D_MOGE_PATH=$WORKSPACE/py_deps/utils3d_moge \
-FASTWAM_DATA_CONFIG=third_party/FastWAM/configs/data/libero_2cam_cosmos.yaml \
-FASTWAM_DATASET_DIRS=$DATA_ROOT/libero_spatial_no_noops_lerobot:$DATA_ROOT/libero_object_no_noops_lerobot:$DATA_ROOT/libero_goal_no_noops_lerobot:$DATA_ROOT/libero_10_no_noops_lerobot \
-FASTWAM_TEXT_EMBEDDING_CACHE_DIR=$TEXT_CACHE \
-FASTWAM_PRETRAINED_NORM_STATS=$NORM_STATS \
-OUTPUT_DIR=$OUTPUT_DIR \
-bash scripts/qwen3_vl_semantic_planner/lingbot_dino_4b/train_lingbot_current_future_fastwam_k1.sh
--- a/tests/test_lingbot_k1_current_future.py
+++ b/tests/test_lingbot_k1_current_future.py
@@ -236,52 +236,6 @@
     assert wrapper.current_depth_head.num_latent_per_keyframe == 64
 
 
-def test_independent_query_pod_launcher_pins_fair_ablation_contract():
-    launcher_dir = (
-        ROOT / "scripts/qwen3_vl_semantic_planner/lingbot_dino_4b"
-    )
-    generic = (launcher_dir / "train_lingbot_dino_4b.sh").read_text()
-    launcher = (
-        launcher_dir
-        / "train_lingbot_independent_queries_fastwam_k1_pod30274.sh"
-    ).read_text()
-
-    assert "INDEPENDENT_MODALITY_TASK_TOKENS" in generic
-    assert "--independent-modality-task-tokens" in generic
-    assert "export INDEPENDENT_MODALITY_TASK_TOKENS=1" in launcher
-    assert "NUM_TASK_TOKENS=64" in launcher
-    assert "four_independent_groups=4x64" in launcher
-    assert "latent_len=256" in launcher
-    assert "BATCH_SIZE=${BATCH_SIZE:-4}" in launcher
-    assert "GRAD_ACCUM=${GRAD_ACCUM:-4}" in launcher
-    assert "PY=${PY:-$ROOT/conda_envs/vlm4wam/bin/python}" in launcher
-    assert "PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True" in launcher
-    assert "MAX_STEPS=${MAX_STEPS:-12000}" in launcher
-    assert "independent_queries" in launcher
-
-
-def test_independent_query_hpc3_launcher_pins_64_tokens_per_feature():
-    launcher_dir = (
-        ROOT / "scripts/qwen3_vl_semantic_planner/lingbot_dino_4b"
-    )
-    current_future = (
-        launcher_dir / "train_lingbot_current_future_fastwam_k1.sh"
-    ).read_text()
-    launcher = (
-        launcher_dir
-        / "train_lingbot_independent_queries_fastwam_k1_hpc3.sbatch"
-    ).read_text()
-
-    assert "export NUM_TASK_TOKENS=${NUM_TASK_TOKENS:-8}" in current_future
-    assert "NUM_TASK_TOKENS=64" in launcher
-    assert "four_independent_groups=4x64" in launcher
-    assert "latent_len=256" in launcher
-    assert "BATCH_SIZE=${BATCH_SIZE:-16}" in launcher
-    assert "GRAD_ACCUM=${GRAD_ACCUM:-1}" in launcher
-    assert "MAX_STEPS=${MAX_STEPS:-12000}" in launcher
-    assert "gradient_checkpointing=off" in launcher
-
-
 def test_depth_warmstart_does_not_accidentally_load_future_depth_head():
     module = load_module(
         "k1_lingbot_head",
--- a/tests/test_lingbot_dino_depth_contract.py
+++ b/tests/test_lingbot_dino_depth_contract.py
@@ -1141,28 +1141,6 @@
     )
 
 
-def test_fastwam_launcher_pins_nine_frame_dual_branch_contract():
-    launcher = (
-        ROOT
-        / 'scripts/qwen3_vl_semantic_planner/lingbot_dino_4b'
-        / 'train_lingbot_dino_depth_fastwam_k4.sh'
-    ).read_text()
-    required_exports = (
-        'export USE_DEPTH=1',
-        'export SEQUENCE_LENGTH=9',
-        'export NUM_KEYFRAMES=4',
-        'export GRID_SIZE=16',
-        'export SEMANTIC_DIM=1024',
-        'export KEYFRAME_SCHEME=even_future',
-        'export SHARED_LATENT_PER_KEYFRAME=32',
-        'export PRIVATE_LATENT_PER_KEYFRAME=32',
-        'export FASTWAM_DATA_CONFIG=',
-    )
-    for export in required_exports:
-        assert export in launcher
-    assert 'train_lingbot_dino_4b.sh' in launcher
-
-
 def test_base_launcher_exposes_in_repo_fastwam_package():
     launcher = (
         ROOT
@@ -1299,7 +1277,6 @@
 
     assert result.returncode == 0, result.stderr
 
-
 def test_fastwam_default_work_dir_is_created_before_stats_write(tmp_path):
     fastwam_src = ROOT / "third_party/FastWAM/src"
     starvla_python = Path("/data/LFT-W02_data/.conda/envs/starVLA/bin/python")
@@ -1336,14 +1313,3 @@
     )
 
     assert result.returncode == 0, result.stderr
-
-
-def test_hpc3_launcher_defaults_to_recommended_12k_budget():
-    launcher = (
-        ROOT
-        / "scripts/qwen3_vl_semantic_planner/lingbot_dino_4b"
-        / "train_lingbot_dino_depth_fastwam_k4_hpc3.sbatch"
-    ).read_text(encoding="utf-8")
-
-    assert "MAX_STEPS=${MAX_STEPS:-12000}" in launcher
-    assert "gbs128_${MAX_STEPS}step" in launcher
--- a/tests/test_lingbot_zero2_runtime.py
+++ b/tests/test_lingbot_zero2_runtime.py
@@ -229,4 +229,15 @@
     assert "GRAD_ACCUM=${GRAD_ACCUM:-2}" in generic
     assert "train_lingbot_dino_4b.sh" in pod
     assert "train_lingbot_dino_4b.sh" in hpc
-    assert "train_lingbot_current_future_fastwam_k1.sh" not in pod + hpc
+
+
+def test_readme_smoke_command_matches_single_gpu_runtime_contract():
+    readme = (
+        ROOT / "scripts/qwen3_vl_semantic_planner/README.md"
+    ).read_text(encoding="utf-8")
+    smoke_contract = (
+        "USE_DEEPSPEED=0 NUM_GPUS=1 BATCH_SIZE=1 GRAD_ACCUM=1 "
+        "EXPECTED_GLOBAL_BATCH=1 MAX_STEPS=2 SAVE_STEPS=2 FULL_FINETUNE=0"
+    )
+
+    assert smoke_contract in readme

