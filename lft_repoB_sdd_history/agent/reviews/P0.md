# P0 Review Findings

### FastWAM dataset adapter is absent from the vendored GE-Act tree

- Severity: P0
- Status: Open
- File: [video_model_libero_fastwam_mix.yaml](../../ge_act/configs/ltx_model/libero/video_model_libero_fastwam_mix.yaml#L11)
- Problem: Both train and validation configs import `data/lerobot_like_dataset.py`, but `ge_act/data/` is absent. The repository-level `data/` ignore rule also prevents the advertised vendored dataset code from being tracked.
- Impact: The current FastWAM LTX training command fails before dataset construction, so neither the baseline nor the proposed semantic-guidance run is launchable from this checkout.
- Evidence: Running `import_custom_class("CustomLeRobotDataset", "data/lerobot_like_dataset.py")` from `ge_act/` raises `FileNotFoundError`; `git check-ignore -v ge_act/data/lerobot_like_dataset.py` resolves to `.gitignore:46:data/`.
