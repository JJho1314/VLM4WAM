# B 线 SDD 开发过程记录（未跟踪内容抢救）

来自 `workspace/VLM4WAM` 的 `.superpowers/` `.agent/` `.agents/` `.codex/`，
在把该 checkout 合并进 `VLA_WM/VLM4WAM` 并删除前抢救。

`.superpowers/sdd/` 里是 Qwen3.5 Baton 从 2026-07-27 到 08-05 的逐任务简报、
完成报告、review diff 和阶段快照 —— 这些在 git 历史里没有，只存在于该工作区的
被忽略目录中。
